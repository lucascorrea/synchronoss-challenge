language: swift
osx_image: xcode9
xcode_workspace: synchronoss.xcworkspace
xcode_scheme: SynchronossTests
before_install:
    - gem install cocoapods --no-rdoc --no-ri --no-document --quiet
    - gem install xcpretty --no-rdoc --no-ri --no-document --quiet
script:
    - xcodebuild -workspace synchronoss.xcworkspace -list
    - xcodebuild -workspace synchronoss.xcworkspace -scheme synchronoss -sdk iphonesimulator -enableCodeCoverage YES -destination 'platform=iOS Simulator,name=iPhone 5s,OS=11.0' GCC_GENERATE_TEST_COVERAGE_FILES=YES build test
